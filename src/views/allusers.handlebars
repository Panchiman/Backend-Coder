<h1>Usuarios:</h1>
    {{#each users}}
    <div>
        <h4><a href="http://localhost:8080/api/users/{{this._id}}">{{this.first_name}} {{this.last_name}}</a> &nbsp; </h4>
        <h5>Email: {{this.email}}</h5>
        <h5>Rol: {{this.role}}</h5>
        <hr>
    </div>
    <br/>
    {{/each}}